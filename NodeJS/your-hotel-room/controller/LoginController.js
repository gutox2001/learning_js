export default class LoginController {
    constructor () {
        this.currentUser = null;
    }

    function login(email, password) {
        // Check if the user exists
        // If the user exists, set the currentUser
        // If the user does not exist, return an error message
    }
} 